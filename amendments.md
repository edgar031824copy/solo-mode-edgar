## Amendments

| ID   | Description | Target Epic | Status |
|------|-------------|-------------|--------|
| F-24 | Migrate file storage from local disk to S3 — CV and LinkedIn uploads written to S3 on candidate creation; pre-screen reads from S3 instead of local disk. Eliminates file loss on Lightsail instance rebuild and enables future horizontal scaling. BE: swap multer disk storage for multer-s3 or direct S3 SDK upload; store S3 key instead of filename. DevOps: provision a private S3 uploads bucket in Terraform (separate from the frontend static bucket). Env vars: `AWS_UPLOADS_BUCKET`, `AWS_REGION` | 6 | done |
| F-25 | Fix CI pipeline — backend tests fail in CI with 401 on all protected routes because test helpers do not include JWT auth headers. Add a shared test utility that generates a valid JWT token and attaches it to requests. All 42 failing tests across candidates, health, screening, postScreening, and report suites must pass in CI. | 7 | done |
| F-26 | Fix CI/CD deploy.yml — (1) replace git pull with rsync for Lightsail BE deploy (Lightsail was bootstrapped manually, not via git clone, so git pull fails); (2) make FE deploy sequential after BE using needs: [deploy-backend] so FE only deploys if BE succeeds, preventing partial prod deployments that break the live app. | 8 | done |
| F-27 | Fix BE CI deploy so it never fails due to missing build tooling: (1) add a build step on the CI runner (npm ci + npm run build) before rsync so TypeScript is compiled in CI, not on Lightsail — a tsc failure aborts the deploy before touching production; (2) exclude node_modules from rsync (Lightsail installs its own clean production deps via npm ci --omit=dev on the server); (3) move prisma from devDependencies to dependencies in apps/backend/package.json so prisma migrate deploy works after npm ci --omit=dev on Lightsail; (4) fix DevOps post-launch agent prompt to always commit all framework state files (docs/.phase, amendments.md, agent-memory files) alongside the deploy commit so no loose changes are left after each epic. | 9 | done |
| F-28 | Fix test isolation: vitest runs must use a local PostgreSQL database instead of the shared Supabase production database. Root cause: vitest.config.ts loads .env via dotenv/config in setupFiles; .env has the production Supabase DATABASE_URL; afterEach hooks in candidates.test.ts, screening.test.ts, postScreening.test.ts, and report.test.ts call prisma.candidate.deleteMany() which wipes all production candidates on every local npm test run. CI is already safe (GitHub Actions injects DATABASE_URL into process.env before npm test, so dotenv never overrides it). Fix: (1) create apps/backend/.env.test containing DATABASE_URL=postgresql://localhost/recruitment_test — commit this file (no secrets, safe to commit); (2) create apps/backend/src/tests/setup-env.ts that calls config({ path: '.env.test' }) first (sets DATABASE_URL to local test postgres), then config() (fills remaining vars from .env like JWT_SECRET and ANTHROPIC_API_KEY — dotenv never overrides vars already set so .env's Supabase URL is ignored); (3) update apps/backend/vitest.config.ts: replace setupFiles: ["dotenv/config"] with setupFiles: ["./src/tests/setup-env.ts"]. CI guarantee: GitHub Actions sets DATABASE_URL in process.env before npm test runs; dotenv's config() skips vars already set; CI always uses its own test DB (no changes to .github/workflows/ci.yml or deploy.yml needed). Before running npm test locally (and in agent steps), the test DB must exist: createdb recruitment_test 2>/dev/null \|\| true && DATABASE_URL=postgresql://localhost/recruitment_test npx prisma migrate deploy. Also update 04-techlead-agent.md and 05-qa-agent.md: add a prerequisite step that creates and migrates the local test DB before every npm test invocation. | 10 | done |
| F-29 | Make pre-screening and post-screening async to eliminate CloudFront 60s timeout risk. (1) POST /candidates/:id/pre-screen returns 202 immediately and runs Claude in the background (no await); on success upserts the PreScreening record as before; on failure sets a new preScreeningError string field on the Candidate model. (2) Same pattern for POST /candidates/:id/post-screen. (3) Frontend polls GET /candidates/:id every 3s after triggering pre-screen or post-screen; stops polling when candidate.status advances (pending → screened for pre-screen, screened → decided for post-screen) or when preScreeningError is set; shows a spinner with "Analyzing..." during polling; displays "Pre-screening failed. Please try again." on error. No new endpoints — reuse existing GET /candidates/:id. Polling costs zero Anthropic tokens (DB read only). Schema change: add optional preScreeningError String? field to Candidate model in prisma/schema.prisma. | 11 | done |
| F-30 | Fix polling bug in pre-screening and post-screening: `CandidateDetailPage.fetchCandidate()` calls `setLoading(true)` on every invocation, which causes React to render the skeleton and unmount the tab component (`PreScreeningTab` / `PostScreeningTab`). Unmounting fires the cleanup effect that calls `stopPolling()`, destroying the interval. The tab remounts in `idle` state because `preScreening` is still null at that moment — polling never resumes. Fix: add a `silent` boolean parameter to `fetchCandidate` (default `false`); when `true`, skip both `setLoading(true)` and `setLoading(false)`. Pass `() => fetchCandidate(true)` as `onRefresh` to both `PreScreeningTab` and `PostScreeningTab`. Initial page load still passes no argument (shows skeleton). Affects `apps/frontend/src/pages/CandidateDetailPage.tsx` only — no backend changes needed. | 12 | done |
| F-31 | Migrate all infrastructure from personal AWS account (503561459070) to company AWS account (995603457880) and replace static AWS credentials in GitHub Actions with OIDC. This is a full infrastructure migration — the app will be redeployed from scratch on the company account. No FE or BE code changes. DevOps agent executes this entirely. CONTEXT: Company DevOps (Andres Torres Duran) created OIDC role `arn:aws:iam::995603457880:role/gha-aisdlc-deploy-role` with full permissions (S3, CloudFront, Lightsail, IAM, STS). Remote Terraform state bucket already exists: `gorilla-tf-state-995603457880` (us-east-1). All infra creation and destruction must go through GitHub Actions workflows for audit trail — never run terraform locally against the company account. CHANGES REQUIRED — (1) Terraform remote backend: add to `infrastructure/terraform/main.tf` a backend block: `backend "s3" { bucket = "gorilla-tf-state-995603457880" key = "solo-mode/terraform.tfstate" region = "us-east-1" }`. Do NOT migrate local state — the local tfstate references personal account resource IDs that are meaningless in the new account. Run `terraform init -reconfigure` to initialize fresh with empty remote state. (2) Self-hosted runner in Lightsail user_data: the new Lightsail instance must auto-register as a GitHub Actions self-hosted runner at boot. Before running terraform apply, generate a fresh runner registration token: `gh api -X POST repos/gorillalogic/edgar-solo-mode/actions/runners/registration-token --jq .token`. Add a sensitive Terraform variable `runner_registration_token` to `variables.tf`. In `lightsail.tf` user_data, append after the PM2 startup block: download the latest GitHub Actions runner binary, extract it to `/home/ubuntu/actions-runner`, run `chown -R ubuntu:ubuntu /home/ubuntu/actions-runner`, then as ubuntu user run `./config.sh --url https://github.com/gorillalogic/edgar-solo-mode --token <runner_registration_token var> --name lightsail-runner --labels self-hosted --unattended`, then install and start as a systemd service with `./svc.sh install ubuntu && ./svc.sh start`. The runner registration token is valid for 1 hour — generate it immediately before running terraform apply. (3) New `terraform-provision.yml` workflow: trigger `workflow_dispatch` only. `runs-on: ubuntu-latest` (GitHub-hosted — this is a rare manual job, billing impact is minimal). Add `permissions: id-token: write` and `contents: read`. Steps: `aws-actions/configure-aws-credentials@v4` with `role-to-assume: arn:aws:iam::995603457880:role/gha-aisdlc-deploy-role` and `aws-region: us-east-1`, then `terraform init`, then `terraform apply -auto-approve`. Pass `runner_registration_token` via a workflow input so the token can be provided at trigger time. After apply, print outputs: Lightsail static IP, CloudFront domain, S3 frontend bucket name. (4) New `terraform-destroy.yml` workflow: trigger `workflow_dispatch` with a required string input `confirm` — only proceed if the value equals `destroy` (add a shell step that exits 1 if the input does not match). `runs-on: ubuntu-latest`. Same OIDC setup as provision. Runs `terraform destroy -auto-approve`. (5) Update `deploy.yml`: add `permissions: id-token: write` and `contents: read` at the `deploy-frontend` job level. Remove the three inline `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_DEFAULT_REGION` env var blocks from all three frontend steps. Add a new step before the S3 sync step: `aws-actions/configure-aws-credentials@v4` with `role-to-assume: arn:aws:iam::995603457880:role/gha-aisdlc-deploy-role` and `aws-region: us-east-1`. The `deploy-backend` job needs no auth changes — it uses local rsync and PM2 with no AWS calls. (6) GitHub secrets — after terraform apply completes and outputs are available: DELETE `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` (replaced by OIDC — no longer needed anywhere). UPDATE `FRONTEND_BUCKET` to the new bucket name from Terraform output (will be `solo-mode-frontend-995603457880`). UPDATE `CLOUDFRONT_DISTRIBUTION_ID` to the new distribution ID from Terraform output. UPDATE `VITE_API_URL` to `http://<new-lightsail-static-ip>:3000`. Keep all other secrets unchanged: `DATABASE_URL`, `JWT_SECRET`, `ANTHROPIC_API_KEY`, `AWS_REGION`, `AWS_UPLOADS_BUCKET`. EXECUTION ORDER — the DevOps agent must follow this exact sequence: (a) pre-check: verify the OIDC role can create IAM users by running `aws iam get-role --role-name gha-aisdlc-deploy-role` and confirming iam:CreateUser is not denied (Terraform creates `solo-mode-backend-s3` IAM user in s3_uploads.tf — if IAM user creation is blocked, the whole apply fails); (b) add remote backend to main.tf; (c) add runner_registration_token variable to variables.tf and terraform.tfvars; (d) add runner setup block to lightsail.tf user_data; (e) add terraform-provision.yml and terraform-destroy.yml; (f) update deploy.yml; (g) commit and push all changes to main; (h) generate fresh runner registration token; (i) manually trigger terraform-provision.yml with the token as input; (j) wait for workflow to complete — poll new Lightsail /health until 200, poll new CloudFront URL until `<div id="root">` appears; (k) update the 5 GitHub secrets listed above; (l) push a no-op commit to trigger deploy.yml and verify the new self-hosted runner picks it up; (m) verify login at new CloudFront URL with recruiter@gorilla.com / password123 and confirm pre-screening works end to end. KNOWN RISK: the existing old self-hosted runner (on personal Lightsail 34.226.38.150) and the new runner both have the label `self-hosted` — GitHub may assign jobs to either during the transition window. After the new runner is verified healthy, deregister the old runner: `gh api -X DELETE repos/gorillalogic/edgar-solo-mode/actions/runners/<old-runner-id>`. Get the old runner ID with `gh api repos/gorillalogic/edgar-solo-mode/actions/runners --jq ".runners[] | select(.name==\"lightsail-runner\") | .id"`. | 13 | done |
| F-32 | Fix HTTPS mixed-content block: frontend is served over HTTPS from CloudFront but `VITE_API_URL` points to `http://<lightsail-ip>:3000` (plain HTTP). Browsers block HTTPS pages from making HTTP requests, so all API calls silently fail with 0 response headers — login and every other feature is broken. Fix: add a second CloudFront distribution in front of the Lightsail backend. Terraform: add `aws_cloudfront_distribution` resource in `infrastructure/terraform/cloudfront_api.tf` with the Lightsail static IP as a custom HTTP origin (port 3000), `AllowedMethods = ["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"]`, `CachePolicyId = "4135ea2d-6df8-44a3-9df3-4b5a84be39ad"` (CachingDisabled managed policy), `OriginRequestPolicyId = "b689b0a8-53d0-40ab-baf2-68738e2966ac"` (AllViewerExceptHostHeader — required so Host header doesn't confuse Express). Output the new domain as `api_cloudfront_domain`. Run terraform-provision.yml after adding the resource. After apply: update GitHub secret `VITE_API_URL` to `https://<api-cloudfront-domain>` and update `CORS_ORIGIN` secret to `https://d3a8iu1mf8poh.cloudfront.net`. Trigger a new deploy after secrets update. No FE or BE code changes needed — Terraform + secrets only. | 14 | done |
| F-33 | Fix CORS_ORIGIN env var not propagating to PM2 in deploy.yml. Root cause: the "Write production environment variables" step writes `CORS_ORIGIN` (and all other secrets) to `/etc/environment`, but the next step's `pm2 restart --update-env` runs in a new shell subprocess that never sources `/etc/environment` — PM2 ends up with a stale `CORS_ORIGIN` from the original user_data bootstrap. Symptom: backend returns `access-control-allow-origin: https://d36qh2h56p23nc.cloudfront.net` (old domain) instead of the current frontend CloudFront URL, causing login and all API calls to fail with CORS error. Fix: in `.github/workflows/deploy.yml`, in the "Install production deps, migrate, restart PM2" step, add `set -a; source /etc/environment; set +a` immediately before the `pm2 restart ecosystem.config.cjs --update-env` line. This makes PM2 pick up the freshly written env vars (including `CORS_ORIGIN`) on every deploy. No Terraform, no FE changes, no new secrets needed. Prerequisite (manual, cannot be done by agent): update the `CORS_ORIGIN` GitHub secret to `https://d3a8iu1mf8poh.cloudfront.net` before triggering the deploy. | 15 | done |
| F-34 | Fix S3 credential env var name mismatch causing AccessDenied on every file upload. Root cause: `deploy.yml` writes S3 IAM credentials as `BACKEND_S3_ACCESS_KEY_ID` and `BACKEND_S3_SECRET_ACCESS_KEY` to both `/etc/environment` and the app `.env` file, but `apps/backend/src/services/s3.service.ts` initializes `S3Client` with no explicit credentials — the AWS SDK auto-reads the standard `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` names which are never set. Mismatch means every upload gets `AccessDenied`. Symptom: POST /candidates with a CV or LinkedIn PDF returns 500 "Internal server error". Fix: update `s3.service.ts` to pass credentials explicitly to `S3Client`: `new S3Client({ region: process.env.AWS_REGION ?? 'us-east-1', credentials: { accessKeyId: process.env.BACKEND_S3_ACCESS_KEY_ID ?? '', secretAccessKey: process.env.BACKEND_S3_SECRET_ACCESS_KEY ?? '' } })`. BE-only change — no deploy.yml edits, no Terraform, no new secrets, no FE changes. | 16 | done |
